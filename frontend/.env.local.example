# Base URL of the Career Agent backend API.
# Copy this file to .env.local and adjust if the backend runs elsewhere.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# API key for the frontend to authenticate against the backend.
# Must match one of the keys in the backend's API_KEYS config.
NEXT_PUBLIC_API_KEY=
